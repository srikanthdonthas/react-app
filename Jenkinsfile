pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                echo 'started'
                sh 'npm install' 
                sh 'ls'
                cmd 'mkdir /%username%\Desktop\DSK'
                sh 'ls'
            }
        }
    }
}