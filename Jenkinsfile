pipeline {
    agent any
    stages {
        stage('Build') { 
            steps {
                echo 'started'
                sh 'npm install' 
                sh 'ls'
                sh 'mkdir C:/Users/%username%/Desktop/DSK'
                sh 'ls'
            }
        }
    }
}